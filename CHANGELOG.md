# 更新记录

## [0.1.1] - 2026-09-21

### 功能

- 关闭主窗口时隐藏到系统托盘并保持后台同步，通过托盘右键菜单“退出”结束进程。
- 支持左键单击托盘、托盘菜单“显示主窗口”或再次启动程序恢复窗口。
- 保留窗口大小和位置，避免从托盘退出后下次启动仍处于隐藏状态。

### 发行

- 提供 Windows x64 安装包和免安装程序。
- 本次未构建 macOS/Linux 附件；对应平台仍可使用 `v0.1.0` 的旧版发行包。

## [0.1.0] - 2026-09-20

首个 Windows 发行版本。

### 功能

- 读取 pi 本地 JSONL 会话并按项目分组。
- 搜索、收藏、归档、恢复和批量重命名会话。
- 查看 Markdown、代码、思考过程、工具调用和分支记录。
- 通过 `--session` 恢复会话，或通过 `--fork` 分叉后继续。
- 监听会话文件变化并自动更新界面。
- 导出单个 JSONL 或多会话 JSON 包。
- 默认优先使用 PowerShell 7，并支持在设置中选择终端。
- 支持读取超过 256 MiB 的会话文件，同时限制单条记录和详情预览内存。

### 验证

- Rust 测试通过。
- TypeScript 和格式检查通过。
- Windows WebView2 桌面端测试通过。
